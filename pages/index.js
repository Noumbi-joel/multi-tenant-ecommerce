import React, { useEffect } from "react";

import firebase from "../firebase.config";

const Welcome = () => {
  return <div>Go to /sign in or /sign up then /dashboard</div>;
};

export default Welcome;
